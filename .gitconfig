[core]
    excludesfile = "~/.gitignore_"
[user]
	name = Éric Lemoine
	email = eric.lemoine@gmail.com
[color]
    status = auto
    diff = auto
[merge]
	tool = vimdiff
[alias]
    addremove = !git add . && git ls-files --deleted | xargs --no-run-if-empty git rm
    st = status
    ls = ls-files
    out = log HEAD..origin
    res = reset --hard
    lsud = ls-files -o --directory --no-empty-directory
#    externals = !git svn show-externals | grep "^/" | sed "s/toto/tata/"
#    externals = !git svn show-externals | grep "^/" | sed "s|^/\([^ ]*\)\(.*\) \(.*\)|(mkdir -p \1 \&\& cd \1 \&\& svn co \2 \3 .)|" | sh
